# Обновления проекта

## 2025-09-02 — Установка зависимостей и аудит безопасности
### Изменения:
- Установлены зависимости строго по lock-файлам (`npm ci`) в корне, `client`, а также сервисах: `server/asterisk_server`, `server/AW`, `server/CRM-server`, `server/dealer-server`, `server/email-service`, `server/register`, `server/telegram_dealer_bot`, `server/tg-bot-server`.
- Выполнён аудит и безопасные исправления (`npm audit` / `npm audit fix`) без форс-обновлений.

### Результат:
- Критические уязвимости устранены во всех пакетах, за исключением `server/telegram_dealer_bot`, где остаются уязвимости, зависящие от устаревшего `request` и `xlsx` (не устраняются без ломающих обновлений или замены пакетов).
- Для `client` остаются некоторые «high» уязвимости в транзитивных пакетах (`node-forge`, `xlsx`, `esbuild`) без безопасных фиксов.
- Изменений в исходном коде не вносилось. Порты и конфигурации не менялись.

## 2025-09-02 — Справка по Канбан (модальное окно)
### Изменения:
- Добавлено модальное окно справки `HelpModalKanban` с локальным поиском по разделам, шагам и иконкам.
- В `Boards.jsx` добавлена кнопка помощи (иконка вопроса) для вызова справки.
- Использованы существующие стили модальных окон (`HelpModal.scss`) для единообразия.

### Результат:
- Пользователь получает подробную справку по Канбан-доске непосредственно в интерфейсе, без смены маршрута.
- Бизнес-логика и порты не изменялись.

### Обновление:
- Справка расширена в повествовательной форме: добавлены разделы про различия задач и уведомлений, роли и видимость, подробные статусы, правила DnD, действия, продление сроков, историю уведомлений, FAQ с нюансами. Добавлены вкладки (категории) внутри модального окна: «Канбан доска», «Карточка задачи», «Уведомление», «Создать задачу», «FAQ».
 - Справка расширена: добавлены разделы Sidebar (иерархия видимости: руководитель видит только подчинённых), Контекстное меню карточки, Лист задач (поиск, фильтры, массовые операции), Проекты, История уведомлений, Менеджер уведомлений. В разделах отображаются иконки с расшифровками, полномочия и сценарии использования.

## 2025-09-02 — Скачивание репозитория
### Изменения:
- Выполнено клонирование репозитория `https://github.com/AlexanderMizintsow/SVAROG_CRM_cursor_v1.3.git` в текущую рабочую директорию.

### Результат:
- Проект успешно загружен. Изменений в коде не вносилось.

## 2024 - Эффект размытия фона в CompletedNotificationsHistory

### Изменения в CompletedNotificationsHistory.scss:
- Добавлен backdrop-filter: blur(8px) для основного оверлея
- Добавлен backdrop-filter: blur(12px) для модального окна с деталями  
- Добавлен backdrop-filter: blur(16px) для основного модального окна
- Добавлен backdrop-filter: blur(20px) для детального модального окна
- Добавлен backdrop-filter: blur(10px) для заголовков модальных окон
- Изменен фон модальных окон на полупрозрачный (rgba(255, 255, 255, 0.95))
- Добавлены тонкие границы для лучшего визуального эффекта
- Добавлена поддержка -webkit-backdrop-filter для совместимости с Safari

### Результат:
Создан современный эффект размытия фона (blur) для всех модальных окон в компоненте CompletedNotificationsHistory, что улучшает визуальное восприятие и создает более профессиональный внешний вид интерфейса.
